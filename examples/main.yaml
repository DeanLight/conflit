_compose:
  - common.yaml
  - path: production.yaml
    into: environments.production

description: "Example layered config with compose, merge tags, and YAML anchors"
app_name: conflit-demo
service: !merge
  features: !merge
    flags: !append
      - notebooks
