site_name: conflit
repo_name: Dean Light/conflit

theme:
  name: material
  features:
    - navigation.sections
    - navigation.top
    - content.code.copy

plugins:
  - search
  - mkdocstrings:
      handlers:
        python:
          paths: [.]
          options:
            show_source: true
            show_root_heading: true
  - mkdocs-jupyter:
      include: ["**/*.ipynb"]
      ignore_h1_titles: false

nav:
  - Home: index.md
